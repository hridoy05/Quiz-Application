import React from 'react';
import UserLayout from '../../components/layout/UserLayout';

const quiz = () => {
    return (
        <UserLayout>
            question
        </UserLayout>
    );
};

export default quiz;